# Finder Accessibility Fixtures

This directory is reserved for real Finder AX snapshots captured from the local machine.

For this batch, only the directory scaffold is created. No synthetic AX fixture is checked in here because later blueprint work calls for real captured data rather than invented samples.

Suggested future naming:

- `finder-list-row-YYYYMMDD-HHMMSS.json`
- `finder-list-row-YYYYMMDD-HHMMSS.txt`
