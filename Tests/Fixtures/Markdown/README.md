# Markdown Fixtures

These fixtures are used for local smoke checks and future automated rendering tests.

- `basic.md` exercises headings, inline code, links, and fenced code.
- `cjk.md` exercises UTF-8 and simple list rendering.
- `not-markdown.txt` is the negative smoke fixture and should never produce a preview.
