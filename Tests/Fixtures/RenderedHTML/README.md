# Rendered HTML Fixtures

These files capture the expected output of the current `MarkdownRenderer` for the paired Markdown fixtures in `Tests/Fixtures/Markdown/`.

Refresh these fixtures whenever the renderer output changes intentionally.
