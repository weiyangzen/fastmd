FastMD Stage 2 macOS adapter crate.

This crate exists to keep Finder as the behavioral reference implementation
while the shared Rust/Tauri layers are introduced. It should not weaken or
replace current macOS behavior unless parity is preserved.
